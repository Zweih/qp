package consts

const (
	DATE       = "date"
	NAME       = "name"
	REASON     = "reason"
	SIZE       = "size"
	VERSION    = "version"
	DEPENDS    = "depends"
	RequiredBy = "required-by"
)

var DefaultColumns = []string{DATE, NAME, REASON, SIZE}
